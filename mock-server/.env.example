PORT=4000

# PostgreSQL – Prisma uses DATABASE_URL only
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=your_username
DB_PASSWORD=
DB_NAME=mock_db

# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE (omit :PASSWORD if none)
DATABASE_URL=postgresql://your_username@localhost:5432/mock_db
